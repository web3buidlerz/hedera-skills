{
  "skill_name": "review-harness-spec",
  "evals": [
    {
      "id": 0,
      "prompt": "Review this harness spec. specs/demo.yaml has name: demo and templateMetadata.name: demo, but validators/demo-static.json asserts template.json name equals my-template. The yarn validator's first command is named yarn-install instead of install. Contract is uncommented but validator.enabled is missing. PRD contains 'C1' and a howToVerify paragraph copied from the contract. Produce a two-axis review.",
      "expected_output": "A review report with ## Wiring and ## Oracle sections. Wiring flags name mismatch, missing install command name, gate 3 misconfiguration, and blind failure. Oracle may note journey/assertion issues.",
      "files": [],
      "assertions": [
        "The output satisfies: Reports a Wiring axis section",
        "The output satisfies: Reports an Oracle axis section",
        "The output satisfies: Flags cross-file name / slug inconsistency",
        "The output satisfies: Flags missing literal install command name",
        "The output satisfies: Flags gate 3 needing both contract and validator.enabled",
        "The output satisfies: Flags oracle assertion text leaking into the PRD"
      ]
    },
    {
      "id": 1,
      "prompt": "Audit whether this harness spec is safe to enable chainValidation. Spec file has contract + validator.enabled, chainValidation.enabled true, network: mainnet, and one assertion with executableWithTestSigner true. Playwright smoke lists deep UX steps that belong in the oracle. Report findings on both axes.",
      "expected_output": "Wiring blocker for network mainnet; Oracle warning that Playwright is too thick; confirmation that executableWithTestSigner requires chainValidation (present) but network must be testnet.",
      "files": [],
      "assertions": [
        "The output satisfies: Includes both Wiring and Oracle sections",
        "The output satisfies: Flags non-testnet chainValidation.network",
        "The output includes chainValidation: References chainValidation",
        "The output includes executableWithTestSigner: References executableWithTestSigner",
        "The output satisfies: Warns that deep UX belongs in the oracle not Playwright"
      ]
    },
    {
      "id": 2,
      "prompt": "Check skills: list on specs/demo.yaml which includes hedera-consensus-service and create-harness-spec. Is that correct? Also the PRD has two journeys but the oracle only has C1 for home load — review that.",
      "expected_output": "Oracle axis warns that create-harness-spec must not be vendored into generator runs and that a PRD journey lacks an assertion; hedera-consensus-service is fine.",
      "files": [],
      "assertions": [
        "The output satisfies: Calls out create-harness-spec in skills list",
        "The output includes hedera-consensus-service: Acknowledges generator-facing skill as appropriate",
        "The output satisfies: Checks journey ↔ assertion traceability",
        "The output satisfies: Explains authoring vs generator audience split"
      ]
    },
    {
      "id": 3,
      "prompt": "Review this extend harness spec. .harness/spec.yaml has name: learn-page, no seed, no extend.baseline, validators under .harness/validators/, and templateMetadata.name: hedera-demo. Produce a two-axis review for the extend layout.",
      "expected_output": "A review with ## Wiring and ## Oracle. Wiring flags missing extend.baseline (and install baseline command). Report notes layout=extend. Oracle may note O14/O15 extend checks.",
      "files": [],
      "assertions": [
        "The output satisfies: Reports a Wiring axis section",
        "The output satisfies: Reports an Oracle axis section",
        "The output satisfies: Flags missing extend.baseline",
        "The output satisfies: Recognizes extend / .harness layout"
      ]
    }
  ]
}
